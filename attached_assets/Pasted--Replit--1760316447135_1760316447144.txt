المهمة: عدّل مشروع "دار الحياة - برنامج الحسابات" الموجود الآن على Replit حسب المواصفات التفصيلية والعملية التالية. **لا تغيّر أي وظيفة حالية غير المصارفة المطلوبة** — التعديلات يجب أن تكون مضافة/تحسينية فقط.

# 1 — متطلبات حساب السجائر في كشف حساب المريض
عند فتح شاشة كشف حساب مريض يجب حساب تكلفة السجائر بطريقة صحيحة طبقًا لإعدادات صفحة "السجائر" (Setting page). نفّذ التالي برمجياً:

1. اجلب من جدول settings القيمتين:
   - `cigarette_box_cost` (سعر علبة السجائر بالجنيه)
   - `cigarettes_per_box` (عدد السجائر في العلبة، افتراضي 20)

2. من سجل المريض استخدم الحقل `cigarettes_per_day` (عدد سجائر المريض يومياً).

3. احسب عدد أيام الإقامة بدقة:
   - إذا كان `discharge_date` موجود: `days = (discharge_date - admission_date).days + 1`
   - وإلا: `days = (today - admission_date).days + 1`

4. تكلفة السجائر = `cigarettes_per_day * days * (cigarette_box_cost / cigarettes_per_box)`.
   - اعرض هذه القيمة تحت بند "تكلفة السجائر" في كشف الحساب.
   - ضمن ملخص كشف الحساب اجعل: إقامة + سجائر + أدوية + حلاقة + مغسلة + بوفيه + مصاريف شخصية − المدفوعات = المتبقي.

4.a) إذا كانت صفحة السجائر تسمح بسعر مختلف لكل مريض (حقل `cigarette_box_cost` في سجل المريض)، فاستخدمه أولوية على القيمة العامة.

# 2 — تكبير مساحة أعمدة جداول عرض المرضى وباقي الصفحات
- اجعل العرض الافتراضي لأعمدة كل `QTableWidget` أو `QTableView` أوسع بحيث تظهر البيانات بالكامل:
  - استخدم `table.setColumnWidth(col_index, width)` للحقول المهمة، أو `horizontalHeader().setSectionResizeMode(QHeaderView.Interactive)` مع `setStretchLastSection(False)` ثم قيم افتراضية مثل: الاسم 300px، الهاتف 150px، تاريخ الدخول 120px، الملاحظات 360px.
- طبق التعديل على جداول المرضى، الموظفين، المدفوعات، والمصروفات.
- أضف زر صغير في واجهة كل جدول "Auto-fit" يقوم بضبط الأعمدة تلقائياً على المحتوى (`resizeColumnsToContents()`).

# 3 — جعل الخط Bold في جميع الصفحات
- اجعل الخط الافتراضي للتطبيق بولد:
  - عند تهيئة `QApplication` أضف:
    ```python
    from PyQt5.QtGui import QFont
    app.setFont(QFont("Cairo", 12, QFont.Weight.Bold))
    ```
  - أو إذا المشروع يستخدم PyQt6:
    ```python
    app.setFont(QFont("Cairo", 12, QFont.Weight.Bold))
    ```
- كما عدّل ملفات CSS/Style لتطبيق وزن خط بولد (`font-weight: 700;`) على العناوين والنصوص الأساسية.
- تأكد أن جميع `QLabel`, `QPushButton`, `QTableWidget` ترث هذا الأسلوب.

# 4 — بحث عن المرضى والموظفين
- أضف شريط بحث (QLineEdit) أعلى جداول المرضى والموظفين.
- ربط الحدث `textChanged` بدالة فلترة التي تقوم بفلترة الجدول بالاعتماد على:
  - اسم المريض/الموظف (partial matches)
  - الرقم القومي أو الهاتف
- البحث يجب أن يكون حساساً للغة العربية (لا تحتاج bidi هنا، مجرد substring match باستخدام `LIKE` في SQL مع `COLLATE NOCASE` أو التعامل عبر Python: `if query in name:` بعد تحويل النصوص إلى نفس الحالة).

# 5 — نظام فلترة وترتيب عرض صفحة المرضى
- أضف عناصر واجهة للفرز/الفلترة بالقرب من شريط البحث:
  - Dropdown أو Toggle يسمى "الترتيب" مع الخيارات:
    - "أبجدي (صاعد)" — sort by full_name ASC
    - "أبجدي (تنازلي)" — full_name DESC
    - "الأقدم أولًا (تاريخ الدخول)" — admission_date ASC
    - "الأحدث أولًا (تاريخ الدخول)" — admission_date DESC
- عند تغيير الاختيار يتم إعادة تحميل الجدول مع الترتيب الجديد (SQL `ORDER BY` أو Python sort).
- أضف فلتر حالة: Active / Discharged / All.

# 6 — عمليات حذف وتعديل وتخريج المرضى، وحذف/تعديل الموظفين، حذف/تعديل المصروفات والمدفوعات
- لكل صف في جداول المرضى/الموظفين/المصروفات/المدفوعات أضف عمود "أفعال" يحتوي أزرار صغيرة: تعديل (✏️)، حذف (🗑️)، (للمرضى) تخريج (🏁).
- عند الضغط Delete أطلب نافذة تأكيد (QMessageBox) نصها بالعربية: "هل أنت متأكد من حذف السجل نهائياً؟ هذا الإجراء لا يمكن التراجع عنه." مع أزرار نعم/لا.
- عند الضغط تعديل افتح النموذج الحالي مع تعبئة القيم الحالية للسماح بالتعديل ثم حفظ التعديل على DB.
- عند الضغط تخريج للمريض: أعطه حقل `discharge_date = today` وحالة `status = 'discharged'` وحدث ذلك في DB، ثم حدث العرض في الجداول وDashboard.
- تأكد من عمل Transactions حول عمليات الحذف/التعديل (begin/commit/rollback) لمنع تلف البيانات.

# 7 — تصميم فريد مقترح لصفحة كشف حساب المرضى وكشف حساب الموظفين
أعطه هذا الوصف ليطبقه أو يصممه من جديد:

**تصميم كشف الحساب — Patient Ledger (و Employee Ledger مشابه)**
- عرض Modal/Window منبثق (نافذة صغيرة مركزية) بعرض 900px وارتفاع 700px، اتجاه RTL.
- أعلى النافذة: شعار المركز (يسار) واسم المريض + رقم الملف (يمين)، تحتهم تاريخ التوليد/تاريخ الحساب.
- قسمان رئيسيان متجاوران (Grid):
  1. العمود الأيسر (عرض 60%):  
     - جدول تفصيلي للعمليات (التاريخ، النوع: دفعة/مصروف، الوصف، المبلغ).  
     - أسفل الجدول صف إجماليات: إجمالي المدفوعات، إجمالي المصروفات، المتبقي.
  2. العمود الأيمن (عرض 40%):  
     - بطاقة معلومات المريض (اسم، رقم قومي، تاريخ الدخول، التكلفة اليومية، عدد السجائر/اليوم).  
     - ملخص سريع: إجمالي إقامة، تكلفة السجائر المحسوبة، تكاليف أدوية، تكاليف إضافية.
     - أزرار: "طباعة HTML" و"تصدير PDF" و"إغلاق".
- استخدام خطوط بولد للعناوين والأرقام المهمة، وألوان هادئة مع تمييز الأرقام الإجمالية بلون أخضر للإيراد وبأحمر للمتبقي إن كان سالب.
- عند النقر على أي عملية في الجدول يمكن فتح تفاصيلها أو حذفها.

# 8 — إنشاء مستخدم جديد وتغيير كلمة سر الأدمن
- أضف في DB (users table) المستخدم الجديد:
  - username = `user`
  - password = hash_bcrypt("1")
  - role = `accountant` (أو اي دور مناسب)
- غيّر كلمة مرور الأدمن الحالية إلى `1231` (مخزنة أيضاً مشفرة بـ bcrypt).
- إذا المشروع لا يدعم hashing، قم بتعديل AuthService لاستخدام `bcrypt` (package: `bcrypt`).
  - قم بتثبيت: `pip install bcrypt`
  - مثال إدخال SQL:
    ```sql
    UPDATE users SET password = '<bcrypt_of_1231>' WHERE username = 'admin';
    INSERT OR REPLACE INTO users (username, password, role) VALUES ('user', '<bcrypt_of_1>', 'accountant');
    ```
- بعد التغيير أعطِ إمكانية للمطور تسجيل الدخول بالمستخدمين لاختبار.

# 9 — استيراد نسخة قاعدة بيانات واستبدال القواعد القديمة
- أضف واجهة "Import Database" تسمح برفع ملف DB (.db / .sqlite).
- عند الاستيراد:
  1. افحص الملف المقبول (validate schema — تأكد أنه يحوي جداول الأساسية).
  2. **احفظ النسخة الحالية في مجلد backup/** مع Timestamp قبل أي حذف.
  3. احذف كل ملفات قواعد البيانات القديمة في مجلد المشروع الجذر بأسماء الامتدادات `.db`, `.sqlite` **باستثناء** الملف الذي تم استيراده (أي استبدال فعلي).
     - تنفيذ: احصل على قائمة الملفات `glob("*.db") + glob("*.sqlite")`, ثم احذف كل ملف غير `imported_filename`.
  4. ضع الملف المستورد باسم موحد `database.db` داخل مجلد `db/` (أو حسب بنية مشروعك).
  5. أعد تهيئة اتصال DB (close + reconnect) حتى يستخدم التطبيق القاعدة الجديدة فورًا.
- أضف تأكيد نهائي قبل الحذف: "سيتم حذف كل قواعد البيانات القديمة والاحتفاظ بقاعدة البيانات الجديدة فقط. متأكد؟" — زر نعم/لا.

# 10 — التحقق والاختبار (QA)
- بعد التعديلات نفّذ سيناريوهات اختبار:
  1. افتح كشف حساب مريض واحسب السجائر يدوياً للتحقق من النتيجة.
  2. إضافة مريض ثم تعديله ثم حذفه.
  3. إضافة موظف ثم تعديله ثم حذفه.
  4. أضافة مصروف ثم تعديله ثم حذفه.
  5. استيراد ملف DB: تحقق من أن كل قواعد البيانات القديمة محذوفة وتم الاحتفاظ بالمستورد فقط، مع وجود نسخة في backup/.
  6. جرب تسجيل الدخول بالمستخدم `user` وكلمة `1`، وبالأدمـن بكلمة `1231`.

# 11 — ملفات للتعديل
اطلب من Replit تعديل/إضافة هذه الملفات أو الدوال (أسماء تقريبية — عدّل بما يتوافق مع مشروعك):
- `db.py` (دوال CRUD، connect_db, replace_db, backup_db)
- `patients.py` (فتح كشف الحساب واحتساب السجائر، UI handlers)
- `ui/patients_view.py` أو `ui_patients.py` (تكبير الأعمدة، إضافة search & filter, buttons column)
- `ui/employees_view.py` (search, edit, delete)
- `ui/ledger_view.py` (تصميم كشف الحساب المقترح)
- `auth.py` (توليد bcrypt hashes، إضافة/تعديل مستخدم)
- `import_db.py` (واجهة رفع واستبدال قواعد البيانات)
- `styles.css` (تأكد من font-weight bold وRTL support)

# 12 — ملاحظات تنفيذية تقنية
- استخدم `arabic-reshaper` + `python-bidi` عند الحاجة لعرض النصوص العربية في Widgets التي لا تعالج RTL جيدًا.
- تأكد من `setLayoutDirection(Qt.RightToLeft)` لجميع النوافذ.
- اجعل `QTableWidgetItem.setTextAlignment(Qt.AlignRight | Qt.AlignVCenter)` لكل العناصر.
- تعامل مع التواريخ باستخدام `datetime.date` واحتساب الأيام بدقة.
- استخدم `bcrypt` لتخزين كلمات المرور.
- قبل حذف أي ملف DB: خزن نسخة احتياطية في `/backup/` مع طابع زمني، ثم احذف الأصلي.

# 13 — رسالة للمطور الذي سينفذ هذا التعديل
"‏التعديل يجب أن يكون non-destructive للمميزات الموجودة — أي لا تغيير في سلوك باقي الشاشات. المطلوب فقط إضافة/تعديل الوظائف أعلاه. اعمل commit واحد شامل بعد الانتهاء مع رسالة commit واضحة: `feat: add patient edit/delete, patient ledger calc, search/filter, DB import & cleanup, add user`."

---

نهاية البرومبت.
